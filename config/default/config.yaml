apiVersion: config.bankdata.dk/v2alpha2
kind: ProjectConfig
#controllerClass:
deletionProtectionDefault: false
#disableCRDWebhooks:
readOnly: true
enableMigrations: false
enableDeltaBundlesDefault: false # This does affect the thingy
#gitCredentials:
logLevel: 0
# leaderElection:
#   leaseDuration: "60s"
#   renewDeadline: "30s"
#   retryPeriod: "5s"
notificationWebhooks: {}
  # systemDatasourceChanged: google.com
  # libraryDatasourceChanged: test.dk
#sentry:
#sso:
styra:
  address: https://styra-url.example.com
  token: styra-token
#  tokenSecretPath: /etc/styra-controller-token/styra_token

opaControlPlane:
  address: https://opa-control-plane-url.example.com
  token: admin-change-me
#systemPrefix:
#systemSuffix:
systemUserRoles:
  - SystemOwner
  - SystemMetadataManager

#datasourceIgnorePatterns:
# - "^systems/[a-z0-9]+/dontdeleteme/.*"
# - "^libraries/[a-z0-9_]+/deletemenot/.*"

#decisionsExporter:

#activityExporter:

podRestart:
  slpRestart:
    enabled: true
    deploymentType: StatefulSet


#opa:
#  decision_logs:
#    request_context:
#      http:
#        headers:
#          - "Accept"

defaultObjectStorage:
  aws:
    bucket: ocp
    region: eu-west-1
    credentials: minio
    url: https://minio.example.com # Optional, only needed for non-AWS S3 compatible storage

defaultRequirements:
  - library1
